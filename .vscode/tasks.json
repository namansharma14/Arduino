{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Competitor Intel",
      "type": "shell",
      "command": "npm install && npm run seed && npm start",
      "options": { "cwd": "${workspaceFolder}/competitor-intel" },
      "isBackground": true,
      "problemMatcher": [],
      "runOptions": { "runOn": "folderOpen" },
      "presentation": { "reveal": "always", "panel": "dedicated", "focus": false }
    }
  ]
}
